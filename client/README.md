# Client

_Bootstraped with Create-React-App_

## Quickstart

```bash
# install dependencies
yarn install

# run in dev mode.
# The `api` server must be running at :3000 port.
yarn start

# build
yarn build

# test
yarn test --verbose
```
